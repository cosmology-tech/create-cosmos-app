import { Layout } from "@/components";
import { Box, Text } from "@interchain-ui/react";

export default function Distribution() {
  return (
    <Layout>
      <Box p="$8">
        <Text fontSize="$2xl" fontWeight="$bold">Distribution</Text>
      </Box>
    </Layout>
  );
}
